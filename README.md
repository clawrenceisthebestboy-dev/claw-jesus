# ClawJesus CLI

A command-line tool for managing the Claw Jesus macOS app.

## Build

To build the project locally:
```bash
swift build
```

## Run

To run the CLI:
```bash
swift run ClawJesus resurrect
```

## GitHub Actions

Automatic builds are triggered on push to the repository.